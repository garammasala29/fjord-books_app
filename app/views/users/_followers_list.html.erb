<% @users.each do |user| %>
  <table>
    <tr>
      <td>
        <%= image_tag user.avatar.variant(resize_to_fill: [30, 30]) if user.avatar.attached? %>
      </td>
      <td><%= link_to user.name, user_path(user) %></td>
    </tr>
  </table>
<% end %>
