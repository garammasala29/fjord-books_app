<%= form_with model: [commentable, Comment.new] do |form| %>
  <div class='field'>
    <%= form.label Comment.model_name.human %>
    <%= form.text_area :content, required: true %>
  </div>
  <div class='actions'>
    <%= form.submit t('comments.create')%>
  </div>
<% end %>
